package com.banktool.loanphoto.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Fluent Design 浅色主题色
 * 与交接文档 THEME 常量完全一致（第 11.3 节）
 * 不含 emoji，系统默认字体
 */
val Bg = Color(0xFFF2F4F8)            // 背景
val Card = Color(0xFFFFFFFF)          // 卡片背景
val Accent = Color(0xFF2196F3)        // 强调色（蓝）
val AccentDark = Color(0xFF1976D2)    // 深强调色
val Text = Color(0xFF212121)          // 主文字
val TextSecondary = Color(0xFF757575) // 次文字
val TextOnLight = Color(0xFFFFFFFF)   // 强调色背景上的文字（白）
val HighlightBg = Color(0xFFE3F2FD)   // 高亮背景
val Success = Color(0xFF4CAF50)       // 成功绿
val Warning = Color(0xFFFF9800)       // 警告橙
val Error = Color(0xFFF44336)         // 错误红
val Divider = Color(0xFFE0E0E0)       // 分割线
val PhotoDoneBg = Color(0xFFC8E6C9)   // 已拍摄条目浅绿背景（Material Green 100，加深以提升对比度）
